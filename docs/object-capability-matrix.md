# Object Capability Matrix

This is the standing audit of every domain object's mutation and access surface — built so capability gaps (a missing tool, a missing test, a missing UI affordance) are discovered systematically, not one object at a time when something breaks. See AGENTS.md → "Object Capability Matrix — Maintenance Rule" for when this file must be updated.

**Origin:** Built 2026-06-30 after discovering Tickets had create-only Orb access (no read/update/delete tool) — a gap that had gone unnoticed because CRUD coverage was being verified piecemeal, object by object, instead of audited as a whole.

**Two axes, not one:**
- **Objects** — the domain entities and system-owned ledgers below. Each has a stable identity across surfaces (DB table → Orb tool → REST → Settings UI → Help → Print → Tests).
- **Flows** — composite interactions that cut across objects (login, dashboard load, voice start). These don't map to a single object, so they get their own matrix with a different column set (see Part 2).

---

## Part 1 — Object Capability Matrix

Legend: ✅ covered · 🟡 covered but unverified/low-confidence · ⚠️ fallback only, not first-class · ❌ gap · — not applicable · `?` unconfirmed with Stan

| Object | DB Table | Orb Tool (C/R/U/D) | `query_db` fallback | REST API (C/R/U/D) | Settings UI (C/R/U/D) | Print | Help | Test Coverage |
|---|---|---|---|---|---|---|---|---|
| **todos** | ✅ `todos`; ORB-337 makes `(product_id, todo_number)` positive, non-null, unique, database-owned, and never recycled within a project lifetime | ✅✅✅✅ Serial: `create_todo`/`query_todos`/`update_todo`/`delete_todo` + `move_todo`, all well-tested. Realtime: exact `get_todo_details`, fail-closed scoped `list_todos`, counts/next-step, and durable single-todo create/update/delete/move/close proposals all confirm through the row-locked RPC. `propose_todo_batch` (2026-07-19) adds a genuine multi-todo create/update/delete/move proposal — one transaction, all-or-nothing, one combined replay-safe receipt — reached when the user names 2+ todos for the same or related action; closing stays single-todo-only (resolution notes + knowledge entry per todo). `query_todos` gained a `category` filter (v0.6.166, 2026-07-08) — todos were always joined with `categories(name)` and shown in output, but had no matching input filter, so a "how many bugs" question could never actually find category-tagged todos server-side. | ✅ | ✅✅✅✅ `/api/tasks` (soft delete); moves retain UUID and receive a trigger-allocated destination address | Primary CRUD lives in the main dashboard (`TodoPanel`/`TodoForm`, via the Unified Dashboard), not Settings — `SettingsProjectTodos` is a project-scoped view, not the canonical editor. Both `TodoPanel` (edit) and `TodoForm` (create) gained a required, searchable `category` field (`ComboSelect`, v0.6.167/v0.6.168, 2026-07-08) — the `category_id` relationship existed and was saved through unchanged on both, but nothing let a user actually set it anywhere in the UI. Backup & Recovery uses a service-role-only restore RPC so exported UUID/address pairs survive a merge while counters advance. | ✅ "Print Backlog" (global nav) | ✅ `ask` topic covers C/R/U/D examples | ✅ Serial Tier 1 + Tier 2 eval cases. Realtime exact/list intent analogues: `exact-task-read-no-invented-blockers`, `query-uses-tool`; ORB-337 adds migration verification for concurrent allocation, move/delete non-reuse, immutability, and restore collision/idempotency. |
| **Todo-number counters** | ✅ `project_todo_number_counters` (RLS; owner/admin diagnostic read; service-role writes; PK/FK `project_id`) | — internal database invariant; all ordinary create/move paths allocate through the trigger | ❌ deliberately excluded | ❌ none | — no management UI; Backup & Recovery calls the service-role-only restore RPC through the existing admin action | — | — | ✅ ORB-337 production migration and structural/security invariants verified; disposable production suite passed concurrent allocation, failed-write rollback, delete/move non-reuse, immutability, restore idempotency/collision/high-water behavior, and cleanup |
| **projects** | ✅ `projects` | ✅✅✅✅ Serial: create/read/update/delete/dormancy with name-first resolution. Realtime ORB-325 adds richer `query_projects`, typed create/update/delete proposals with transactional replay-safe receipts, and verified sleep/wake. Codes remain immutable and generated only by the shared TypeScript generator; delete explicitly hard-deletes every todo. | ✅ bounded `query_db` fallback also available in Realtime | ❌ none | ✅✅✅✅ `SettingsProjects` — confirmed `onEdit`+`onDelete`, create via command-bar button | — | 🟡 generic only (`[project]` placeholder examples in `ask` topic, no dedicated topic) | 🟡 Serial coverage plus Realtime read/create/update/delete/dormancy analogues; direct proposal/RPC wiring uses rollback DB verification + DEV acceptance. |
| **knowledge_repo** | ✅ `knowledge_repo` | ✅create / ✅read / ✅update / ⛔ **no delete by design**. Serial retains `add_knowledge`, ranked/precise `search_knowledge`, and held `update_knowledge`. Realtime ORB-325 Phase 3 adds native ranked topic search, precise title resolution through the shared resolver, attributed proposal-backed create/update, stale snapshot rejection, one audit event, and replay-safe receipts. Delete remains admin-only in Settings; staleness routes to `create_ticket`. | ✅ | ❌ none | ✅✅✅✅ `SettingsKnowledge` — confirmed `onEdit`+`onDelete` (delete remains admin-only, matching the Orb tool boundary) | — | ❌ none | 🟡 Existing serial Tier 1/Tier 2 coverage plus ORB-325 `realtime-search/add/update-knowledge-intent-analogue`; direct Realtime writes use rollback database verification + DEV acceptance. |
| **orb_preferences** | ✅ `orb_preferences` | ✅read(`get_preferences`) / ✅upsert(`set_preference`) in serial and Realtime; server validates the canonical key/value contract | ❌ deliberately excluded | ❌ none | ✅ AI Settings surfaces relevant preferences | — | ❌ none | 🟡 Serial behavior coverage plus `realtime-set-preference-intent-analogue`; direct handler verification manual. |
| **orb_memory** | ✅ `orb_memory` | ✅create(`save_memory`) / ✅read(`recall_memories`) in serial and Realtime; respects `memory_level=off|session|full` and active-expiry scope | ❌ deliberately excluded | ❌ none | ✅ AI Memory | — | ❌ none | 🟡 Existing memory Tier 1 plus Realtime save/recall analogues. |
| **orb_adaptations** | ✅ `orb_adaptations` | ✅proposal create(`propose_adaptation`) in serial and Realtime; activation/rejection remains email/user-controlled, never model-controlled | ❌ deliberately excluded | ✅ approval/rejection links | ✅ AI Memory/adaptation management | — | ❌ none | 🟡 Existing Tier 2 proposal case; direct Realtime email/audit wiring manual. |
| **dev_channel** | ✅ `dev_channel` | ✅ explicit Orb→developer send in serial and Realtime; developer→Orb processing remains existing channel behavior | ❌ deliberately excluded | ✅ `/api/dev-channel` | — | — | ❌ none | 🟡 `realtime-send-developer-intent-analogue`; direct delivery manual. |
| **tickets** | ✅ `tickets` | ✅create / ✅read / ❌update / ❌delete. Serial and Realtime both expose admin-only, two-tier `query_tickets` reads (compact list/filter; full single-code detail). Realtime Phase 4 keeps the boundary explicitly read-only; ticket changes remain Settings-only. | ✅ (RLS-scoped own-ticket fallback for non-admins) | ❌ none | ✅✅✅✅ `SettingsTickets` (admin-managed) | — | ❌ none | 🟡 Existing Tier 1 ticket coverage plus `realtime-query-ticket-intent-analogue`; direct Realtime role/data wiring remains manual. |
| **audit_log** | ✅ `audit_log` | ✅read-only by design. Serial `query_audit_trail` and Realtime Phase 4 `query_audit` support todo code, table, action, date, and bounded results. | ✅ | ❌ none | ✅ read-only by design (`SettingsAudit`, "Open" to view, no mutation actions) | — | ❌ none | 🟡 `realtime-query-audit-intent-analogue`; direct packet verification manual. |
| **categories** | ✅ `categories` | ❌ no Orb tool at all | ✅ | ❌ none | ✅✅✅✅ `SettingsCategories` — `onEdit`+`onDelete`+`canDelete` (guards in-use categories) | — | ❌ none | ❌ none found |
| **groups** | ✅ `groups` | ❌ no Orb tool at all | ✅ | ❌ none | ✅✅✅✅ `SettingsGroups` — `handleAdd`+`editingId`+`confirmDeleteId` confirmed | — | ❌ none | ❌ none found |
| **statuses** | ✅ `statuses` | ❌ no Orb tool | ✅ (read fallback only) | ❌ none | ❌ **no Settings page at all** | — | ❌ none | ❌ none found |
| **priorities** | ✅ `priorities` | ❌ no Orb tool | ✅ (read fallback only) | ❌ none | ❌ **no Settings page at all** | — | ❌ none | ❌ none found |
| **invitations** | ✅ `invitations` | ❌ deliberately excluded (sensitive, per `DB_SCHEMA` comment) | ❌ excluded | ❌ none | ✅ `SettingsInvitations` — `onDelete`+`onResend`+`onCopyDecline` (revoke/resend model, not plain update) | — | ❌ none | ❌ none found |
| **users** | ✅ `users` | ❌ deliberately excluded (sensitive) | ❌ excluded | ❌ none | ✅ `SettingsUsers`/`SettingsUserDetail` — `onEdit`+`onDelete`+`canDelete`; create is via signup/invitation, not direct | — | ❌ none | ❌ none found |
| **performance_events** | ✅ `performance_events` | ❌ no Orb tool; telemetry system-owned | ❌ not in `ALLOWED_TABLES` | 🟡 create-only `/api/performance-events` ingestion endpoint, not external-agent CRUD | ✅✅✅✅ `SettingsPerformance` — full admin CRUD/search/filter/sort/detail for telemetry rows | — | ❌ none | ❌ none found |
| **Realtime mutation proposals** | ✅ `orb_realtime_proposals` (RLS enabled; service-role only; user, pending-expiry, target-todo, and destination-project indexes; executed receipt survives later todo/project deletion; `kind` CHECK includes todo, project, knowledge, and batch todo mutations) | ⚠️ internal Realtime propose/confirm tools for todo create/update/soft-delete/move/close, project create/update/permanent-delete, Knowledge Repository create/update, and (2026-07-19) `propose_todo_batch` for 2+ todo create/update/delete/move operations in one confirmation. Dedicated transactions preserve closing obligations, immutable project-code generation, attribution, stale checks, one audit event per operation, all-or-nothing batch execution, and replay-safe receipts. Natural references resolve server-side; not exposed to `query_db`. | ❌ deliberately excluded | 🟡 authenticated allowlisted internal `/api/orb-realtime/turn`; no external-agent CRUD | — system-owned transaction ledger | — | — | ✅ Todo and project rollback verification passed. Phase 3 adds rollback verification for attributed knowledge create/update, identical replay, audit cardinality, and stale rejection. Batch todo mutations rollback-verified live (2026-07-19): mixed-action batch, uniform-action batch, replay idempotency, and stale-item rejection aborting an otherwise-valid batch. Tier 1 intent analogues cover all routed mutations; direct Realtime execution remains manual. |

### Deliberate exclusions (not gaps)
`users` and `invitations` are intentionally kept out of Orb tools and `query_db` — `lib/db-schema.ts`'s own `DB_SCHEMA` comment marks them `EXCLUDED (sensitive)`. Confirmed by design, not by omission.

### Confirmed deliberate (not a gap)
`statuses` and `priorities` have **zero** surface anywhere (no Orb tool, no Settings page) outside the read-only `query_db` fallback. **Confirmed by Stan 2026-06-30: deliberately meant to stay fixed/unmanaged.** Not a gap — do not propose tools or a Settings page for these without a new explicit request.

### Tracked gaps (filed as ORB todos, 2026-06-30)
- ~~**ORB-301** — Add a read tool for projects (`query_projects`)~~ **Closed v0.6.143 (2026-07-03)** — Tier 1 36/36 green, live-verified on Mac + iPhone
- ~~**ORB-302** — Add update/delete tools for `knowledge_repo` entries~~ **Closed v0.6.149 (2026-07-04)** — update_knowledge + search_knowledge precise-read shipped (delete deliberately excluded, admin-only per Stan); Tier 1 40/40 green, live-verified across two full conversations including a real RLS data-visibility bug found and fixed along the way
- ~~**ORB-303** — Add read/update/delete tools for tickets~~ **Scope narrowed to read-only during planning (2026-07-07)** — `query_tickets` shipped, admin-only. No `update_ticket` — not pursued (open/dismiss/close already covered by the admin Settings UI, and the fuller CRUD framing was superseded by a tighter read-only ask). Also added `tickets` to `query_db`'s `ALLOWED_TABLES` as a complementary, RLS-scoped path for non-admins to ask about their own filed tickets.
- **ORB-303 follow-up (2026-07-08)** — Stan's live testing of `query_tickets` (text and voice) found two real bugs: (1) citing a ticket code from a tool result or the static RECENT TICKETS context tripped the unverified-completion-claim guard and silently replaced a correct answer with a confusing "Correcting..." apology — root cause was the generic tool-output code tracker only reading a top-level `output.code`, never `output.returned[].code` (the shape every list-returning read tool uses), and `historyCodes` never scanning the static BACKLOG/RECENT TICKETS context the way it already did for todo codes; (2) Orb offered to close/update/delete a ticket despite no such tool existing. Both fixed same-day in `orb-converse.ts` (+ mirrored in the eval endpoint where structurally applicable) and the `query_tickets` tool description. Not addable to the eval suite — see the tickets row's Test Coverage cell for why.
- ~~**ORB-318** — Add category dropdown to todo create/edit modal~~ **Closed (2026-07-08)** — Found while diagnosing the bugs/tickets routing gap: the `category_id` relationship on todos existed and was silently carried through on save in both modals, but no UI anywhere let a user actually set it. `TodoPanel` (edit, v0.6.167) and `TodoForm` (create, v0.6.168) both now have a required, searchable category field (`ComboSelect`).
- **Categories per project (2026-07-08):** categories are a per-`product_id` table, not a global enum — CAN26 (a trip-planning project) has 14 highly project-specific categories (Alaska Airlines, Packing, Weather, etc.), Helm has 5 (Feature Request, Business, Coding, QA, Bug), Orb has 9 (iPhone, Bug, QA, Server, Client, Design, Feature, iOS, Mac). Seeded a standard Feature/Bug/Chore/Docs/Support set only into the two projects that had zero categories (`mrstokely-from-boston`, `Pre-todos`) so the new required field is satisfiable there — existing projects' categories were deliberately left untouched, not standardized.

### Cross-cutting finding: test coverage
The **only** test mechanism in this repo is the Orb eval suite (`scripts/eval-cases.ts`), and it exclusively covers **Orb-conversation tool-calling and speech behavior** (Tier 1 / Tier 2). There is **no traditional unit, integration, or E2E test suite** anywhere in the codebase — Settings UI CRUD, REST API, print, and every non-conversational surface in the table above ships with zero automated test coverage. This is a standing gap across nearly every row, not a per-object issue.

**v0.6.217 contract repair:** `query_db` remains the bounded fallback represented by this matrix. Its implementation and routing had survived, but its generated serial tool definition was missing; the canonical `docs/api-spec.yaml` internal-tool schema now exposes its required allowlisted `table` and bounded query parameters again. `realtime-query-db-intent-analogue` guards the serial contract shape used as the Realtime parity analogue.

**Follow-up found in live Realtime testing (2026-07-18):** the Realtime session's own instructions never included `lib/db-schema.ts`'s `DB_SCHEMA` (the serial engine's system prompt has carried it since it existed) — the Realtime model had no ground truth for real column names when reaching for `query_db`, so a request needing an actual column (e.g. "which task has gone longest without an update") could guess a non-existent one and fail at the database layer. Fixed by injecting the same `DB_SCHEMA` into the Realtime session instructions. Separately, the turn route's catch-all error handler only read `.message` from `instanceof Error` objects; a Supabase/Postgrest query error is a plain object with a `.message` but is not an `Error` instance, so its real reason was silently replaced by a generic "Realtime turn failed." — broadened to also read `.message` from any object that has one. `realtime-query-db-schema-column-intent-analogue` guards the schema-dependent case.

**Realtime batch todo mutations (2026-07-19):** naming 2+ todos for the same create/update/delete/move in one utterance (e.g. "delete test1, test2, test3") previously produced independent single-todo proposals with no combined announcement — `executeToolBatch` silently fell back to free-form model narration instead of reciting any of the pending confirmation texts. Fixed two ways: (1) the client now forces ALL pending exact texts verbatim when 2+ proposals exist in one turn, not just one; (2) Realtime gained its own `propose_todo_batch` tool and `confirm_realtime_batch_todo_mutation` RPC — one signed proposal, one transaction, all-or-nothing (any operation's stale check or resolution failure aborts the whole batch before any row is written), one audit row per operation, one combined replay-safe receipt (compact "Deleted 3 todos in X." when every operation shares an action and project, itemized otherwise). Closing is deliberately excluded from batches (matches serial's `todo_action_transaction` scope) — it requires resolution notes and a knowledge entry per todo, which doesn't compress into a combined confirmation. Rollback-verified live: mixed batch (update+delete+move), uniform batch (2 creates), replay idempotency, and stale-item rejection aborting a batch that also contained valid operations. `realtime-propose-todo-batch-intent-analogue` guards the tool-call shape. This is intended as the start of a shared canonical proposal pattern for both engines — see ORB-342 for the fuller serial/Realtime convergence, not attempted here.

---

## Part 2 — Flow / Performance Matrix

Speed is a flow property, not an object property — login isn't one of the 11 domain objects, but it's exactly the kind of critical path this matrix exists to stop losing track of. Per [[project_systematic_quality_audits]]: when one flow is found slow, audit the class, don't patch the instance.

| Flow | Instrumented? | Measured baseline | Budget/target | Known issues |
|---|---|---|---|---|
| **Login/auth → app visible** | ✅ `auth` focus covers login mount, passkey click (stage marks: challenge start, option parsing, browser credential return, serialization, Supabase verify, completion), OTP request, OTP verify. Conditional-mediation (passkey autofill) span **removed** (v0.6.174). New `route_to_ready` span (v0.6.175) measures auth-success → dashboard-ready end-to-end, backdated across the redirect. | ORB-312/309: passkey_click is dominated by the OS credential ceremony (`navigator.credentials.get`) — environment, not app code; OTP request/verify fast. The old multi-minute "auth" durations were conditional-mediation **dwell**, not latency. | Watch, no hard budget yet | Benign passkey outcomes (user-cancelled / no-credential / abort / expired) and the removed conditional-mediation span are now classified as **expected** — excluded from auth failure-rate and latency (v0.6.175); they had inflated auth to a ~30% failure rate. |
| **Initial dashboard load** (products + todos fetch) | ✅ `dashboard-init` covers client profile/projects/priorities/statuses/todo loads, plus `route_to_ready` (v0.6.175) for the perceived auth→ready wait across the redirect | ORB-312: `dashboard-init` p95 ~4.5–5.0s on Mac/iPad (n=240/133) is a real app-side tail; the login→dashboard handoff is now measured as one correlated span rather than falling between unlinked spans | TBD | — |
| **App-shell system-state polling** | ✅ `background / system-state / version_poll` measures the single `/api/version` request that supplies reachability, version, maintenance, lockout, and broadcast state. ORB-326 removed the redundant client `/api/health` request while retaining that endpoint for external probes. | Request count per initial/interval/focus/visibility/online/manual trigger: **1 → 1 `/api/version`, 0 `/api/health`**. Latency baseline pending when background telemetry is enabled. | Exactly one app-shell request per trigger; hidden tabs skip interval ticks; network/non-OK responses set offline; recovery restores online and all system state. | Performance telemetry is opt-in for the `background` focus so ordinary polling does not create measurement traffic. |
| **Project switch** | 🟡 dashboard click instrumentation added for project selection through resulting list-settled timing | Baseline pending | TBD | — |
| **Settings page loads and CRUD** | ✅ central Settings CRUD instrumentation covers initial/search/filter/sort/pagination loads plus modal/add/save/delete/move/bulk actions; Settings navigation and AI Metrics full page load instrumented | ORB-309 production samples exist for AI Metrics, Settings Performance, Audit Log, and Knowledge Repo. AI Request Log exact-count paging improved after cursor pagination; Audit Log and Knowledge Repo were acceptable in July 2026 samples but should be watched as they grow. **ORB-312 pass 1 (v0.6.176):** AI Metrics `ai_accounting_load` was ~100% server-auth (two serial server actions each re-authing via `getUser`), RPC only 183ms; merged into one auth-gated bundle (`getAiMetricsBundle`) → Mac p50 4251→2631 (~38%, confirmed), mobile within noise. | Watch, no hard budget yet | Reusable fetch patterns from ORB-311: cursor pagination for growing detail logs, database-side rollups for large-table summary cards/breakdowns, and honest component-lifecycle full-page timing when nav timestamps are noisy. The single `getUser` network validation per admin action is now the dominant remaining cost (worst on mobile); next pass is local JWT verification — see `docs/orb-312-performance-optimization.md`. |
| **Voice-mode start and turn** (recognition → response → playback → mic return) | ✅ Current serial path retains `voice-recognition / listen_to_submit`, `voice-turn / recognized_to_mic_return`, and `voice-output / greeting_playback|answer_playback`. ORB-325 adds `voice-realtime-spike-start / tap_to_first_webrtc_audio_packet`, `voice-realtime-spike / speech_to_mic_return`, and `voice-realtime-spike-interruption / speech_during_playback` paths with microphone, SDP, data-channel, inbound-RTP first audio, transcript, live fact/action tool, response-done, mic-return, and transcript-present interruption evidence. Task counts require explicit `named_project|all_owned` scope and fail closed instead of widening; named-project counts preserve exact `open|active|parked|all` status scope. The project-directory packet returns exact owned-project count and names; richer project reads resolve a readable owner through the indexed `projects.created_by → users.id` relationship and server-compose owner, dormancy, and description instead of exposing the creator UUID. Todo update/delete/move resolves a natural title/code and optional project name to one fresh row, persists a proposal, applies the shared text/Realtime authorization predicates to the actual current transcript, and requires a database receipt; ambiguity and stale row versions fail closed. Turn IDs abort or quarantine stale results after interruption. One absolute 12s response deadline starts at transcription completion and spans response creation plus verified tool-result handoff without resetting; it cancels/quarantines a stall and returns the session to listening. Browser audio processing, Realtime `far_field` noise reduction, and noise-thresholded server VAD are explicit. Completed transcript telemetry includes VAD audio duration and aggregate ASR log-probability/confidence values without retaining transcript text. The v0.6.203 Silero V5 spike adds `voice-realtime-vad-shadow / initialize|session_runtime` measurements plus per-turn acoustic summaries from the exact WebRTC `MediaStream` on one local WASM thread. v0.6.209 disables provider auto-interruption only in local DEV and classifies playback-coupled input before cancellation: ready Silero evidence with at least 20 frames, no 400ms real start, and ASR confidence below 0.5 cannot truncate Orb, is deleted, and creates no response; clear input cancels the parent and creates one successor only after cancellation completes. Ordinary listening turns and production remain fail-open/provider-controlled. Production endpoints are disabled unless the authenticated email passes the server allowlist; main-button routing remains on the serial fallback until acceptance is explicit. | 2026-07-12 Mac/Safari serial failure case: initial strategic model call **75,437ms** plus first TTS chunk **3,028ms** (minimum first-audio wait ~**78.5s**, excluding context/decode). First Realtime iPad runs: post-transcript median **2.68s**, count/proposal/confirm gateways **395/216/651ms**, one audited create, but two no-transcript ambient interruptions. Server VAD threshold **0.65** then produced zero ambient false triggers across the representative matrix while preserving 7 transcript-bearing interruptions. Cross-browser results: Mac Chrome startup/median response **4.42s/1.63s**; Mac Edge **9.18s/2.96s** (7.6s permission ceremony); iPhone Safari **3.75s/2.17s**. Firefox recorded **1.68s/3.20s** but repeatedly went deaf after a few turns—ultimately without a provider speech event—so it is deferred to ORB-330 and is not an ORB-325 release gate. Edge later produced one false transcript from an apparent cursor/trackpad or ambient trigger. Silero runtime payload is **15,834,162 bytes** before transfer compression/caching. First Mac Chrome shadow run: initialize **375ms**, reported heap delta **+4.18 MB**, then 150.16s at **31.24 frames/sec**, **31.997ms** average interval, **64ms** max gap. Ambient “Но” had Silero max **0.1465**, zero positive frames, ASR confidence **0.0008**; quiet “Yes” had max **0.7289** and eight positive frames; real-speech mistranscription “Goans helm” had max **0.9685** but ASR confidence **0.1755**. A second greeting-coupled false transcript “Měli” had 82 frames, Silero max **0.0257**, zero positives, and ASR confidence **0.0295**; two subsequent silent Mac Chrome starts on speakers/headphones were clean. The v0.6.208 guard then suppressed zero turns: playback-coupled “음” had 8/68 positives and confidence 0.3305, while quiet/backchannel “mhm” had no real start and confidence 0.2279. iPad/iPhone and Safari/Edge client-classified evidence remains pending. | Accepted gate: database Fact Packet ≤400ms warm; simple grounded first audio ≤3s, hard failure >5s; one utterance → one response and legal transition; zero unverified factual claims or duplicate writes; immediate interruption for classified clear speech; transcript/audio agreement. Weak playback-coupled input must not truncate Orb or create a response; clear short speech must cancel once and produce one successor; ordinary quiet/accented/multilingual turns must pass through; deleted provider items and parent/candidate telemetry must complete without error before production enforcement is considered. | Realtime hardening persists all supported todo proposals and confirms through one row-locked database RPC: one todo change, one audit event, one stored canonical receipt returned on replay. Closing is implemented as a dedicated `close_todo` proposal whose transaction requires resolution notes and writes the matching Knowledge Repo entry atomically; the plain update path still cannot set a closed status. No Realtime database subscription, polling, second microphone request, transcript storage, or audio storage is used. Supported verification matrix: Safari, Chrome, Edge. Provider/model/voice, browser/platform, route stages, result state, provider VAD duration, aggregate ASR confidence, Silero summaries, client-classified interruption mode, and suppression/cancellation outcome are metadata. |
| **Todo CRUD round-trip** (confirm → execute → UI update) | 🟡 dashboard click instrumentation covers create, panel save/delete, toggle done, status move, bulk close/delete, sort/filter/view/list controls, and project create/update/delete. ORB-337's counter allocation is inside the existing create/move spans; no per-allocation telemetry write is added. | Baseline pending; compare warm create/move before and after ORB-337 on Mac, iPad, and iPhone | TBD | Counter row locking is intentional and should appear in existing end-to-end timings under concurrent writes. |

**Voice lifecycle telemetry (v0.6.210):** Realtime DEV startup and teardown now record a stable hook-instance ID, monotonically increasing connection generation, start source, and explicit stop reason. In-flight startup is abortable, stale-generation media/data-channel events are ignored, and provider errors release the failed transport before exposing a recoverable error state.

**Tracked:** **ORB-304** — Systematic time-to-interactive instrumentation across critical flows. **ORB-309** — Improve Initialization Speed, the concrete follow-up to measure all user-clickable initialization/interaction paths in both dev and production. Login is the first known case, not the whole scope.

---

## Maintenance

See AGENTS.md → "Object Capability Matrix — Maintenance Rule" and "Performance Instrumentation — Build Rule." Short version: touching any object's mutation surface (new table, new Orb tool, new REST endpoint, new Settings page, new eval case) or any critical flow updates this file in the same change, and new user-facing functionality must explicitly decide whether performance instrumentation is required.
