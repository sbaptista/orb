import SettingsTicketsPrototype from '@/components/settings/SettingsTicketsPrototype'

export const metadata = {
  title: 'Tickets Prototype — Orb Settings',
  description: 'Responsive CRUD table prototype (ORB-221)',
}

export default function TicketsPrototypePage() {
  return <SettingsTicketsPrototype />
}
